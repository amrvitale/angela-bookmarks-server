DROP TABLE IF EXISTS bookmarks;
DROP TYPE IF EXISTS star_rating;

--creates the bookmarks table
-- a SQL migration is a similar to a SQL build script, except that it changes a db from one version
-- to another, rather than build it from scratch.